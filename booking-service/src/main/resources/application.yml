spring:
  datasource:
    url: jdbc:postgresql://localhost:5434/booking_service_db
    username: postgres1
    password: postgres1
  jpa:
    hibernate:
      ddl-auto: none
    open-in-view: false
    show-sql: true
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    org.hibernate.SQL: debug